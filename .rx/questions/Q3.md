---
id: Q3
gap: Canonicalization is often proposed as a robustness fix, but existing normalization
  and context-management work does not establish that it improves paired invariance
  and cross-source transport without discarding useful evidence.
parent_evidence_id: null
---

# Does a neutral canonical event representation reduce verifier instability and cross-generator/harness gaps without degrading native-representation accuracy?

Secondary intervention, conditional on Q1 finding material sensitivity. Map all accepted source schemas to one declared canonical event representation, then compare native versus canonical verifier performance using the same trajectories, labels, verifier prompts, and thresholds. Cross-generator/harness transport is secondary because natural source shifts change content as well as representation.

Four-axis novelty delta versus harness canonicalization and verifier context-management work: framing = canonicalization is judged by invariance recovery and deployment error preservation; mechanism = typed lossless event canonicalizer with round-trip/provenance tests; insight = test whether removing source identity cues improves transport or instead removes decision-relevant evidence; domain = cached terminal-agent verification. Canonicalization alone is not claimed novel.

Prediction/falsifier: H5 predicts lower paired instability and smaller source-conditioned error gaps with no material loss in within-source discrimination/calibration. If canonicalization only compresses context, changes evidence, or lowers native accuracy beyond a locked non-inferiority margin, the mitigation claim fails.

Evidence expectation: a native/canonical paired comparison showing instability, AUROC/calibration, fixed-threshold FPR/FNR, token budget, truncation rate, and source-conditioned gaps, with an ablation separating schema normalization from prose removal. Claim mitigation only if the locked accuracy-retention and robustness criteria both pass.
